# @clerk/clerk-expo declareert een react-dom peer dependency die botst met de react-versie
# van Expo SDK 54. Zonder deze vlag faalt `npm ci` — o.a. in de EAS Build "Install dependencies"
# fase, waar we de vlag niet handmatig kunnen meegeven.
legacy-peer-deps=true
